---
/**
 * Root Redirect Page
 *
 * Redirects the root path to the default language (Italian).
 */
import { DEFAULT_LANGUAGE } from '@marcomarchione/shared';
---

{/* This page redirects to the default language */}
{Astro.redirect(`/${DEFAULT_LANGUAGE}/`)}
