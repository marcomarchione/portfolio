# Bun test configuration
# Limit concurrency to prevent PostgreSQL connection exhaustion

[test]
# Run tests sequentially to prevent database pollution between test files
# Each test file uses its own connection and TRUNCATE, but parallel execution
# can cause race conditions in shared database
concurrency = 1

# Increase timeout for beforeEach hooks (DB reset can be slow)
timeout = 10000
